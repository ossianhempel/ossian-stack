# grilling

Vendored from [mattpocock/skills](https://github.com/mattpocock/skills)
(`skills/productivity/grilling`).

The grilling body skill: a relentless design-tree interview. Model-invocable so
other skills can name it; the `grill-me` and `grill-with-docs` routers are the
user-facing entry points.

To update:

```bash
npx skills add mattpocock/skills -y --skill grilling
```
