# find-animation-opportunities

Vendored from [emilkowalski/skills](https://github.com/emilkowalski/skills) (`skills/find-animation-opportunities`).

Read-only UI sweep for places that would genuinely benefit from motion — and explicit callouts for what not to animate. Pairs with `improve-animations` (audit + plan fixes), `review-animations` (diff review), and `emil-design-eng` (implementation philosophy).

To update:

```bash
npx skills add emilkowalski/skills -y --skill find-animation-opportunities
cp -R .agents/skills/find-animation-opportunities/* skills/find-animation-opportunities/
```
