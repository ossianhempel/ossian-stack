# bro

Vendored from [dmmulroy/skills](https://github.com/dmmulroy/skills) (`bro`).

Restates the previous message in plain human language without jargon. The
skill is intentionally opt-in and only runs when explicitly invoked.

To update:

```bash
npx skills add dmmulroy/skills -y --skill bro
cp -R .agents/skills/bro/* skills/bro/
```
