# better-interface

Vendored from [jakubkrehel/skills](https://github.com/jakubkrehel/skills) (`skills/better-interface`).

Cross-discipline interface review that coordinates the other `/better` skills. Use for holistic audits; pair with `better-ui`, `better-typography`, `better-colors`, `better-accessibility`, `better-layout`, and `better-writing` for focused passes.

To update:

```bash
npx skills add jakubkrehel/skills -y
cp -R .agents/skills/better-interface/* skills/better-interface/
```
