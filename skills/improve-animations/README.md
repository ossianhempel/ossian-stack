# improve-animations

Vendored from [emilkowalski/skills](https://github.com/emilkowalski/skills) (`skills/improve-animations`).

Audit-then-plan advisor for animation and motion code: survey the codebase, produce prioritized findings, and write self-contained implementation plans for other agents (or cheaper models) to execute. Pairs with `review-animations` (diff review), `find-animation-opportunities` (missing motion), and `emil-design-eng` (implementation philosophy).

To update:

```bash
npx skills add emilkowalski/skills -y --skill improve-animations
cp -R .agents/skills/improve-animations/* skills/improve-animations/
```
