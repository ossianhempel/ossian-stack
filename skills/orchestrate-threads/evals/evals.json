{
  "skill_name": "orchestrate-threads",
  "evals": [
    {
      "id": 0,
      "prompt": "/orchestrate-threads gainslog — first task: add CSV export of a user's workouts (share sheet on iOS, one row per set).",
      "expected_output": "HQ bootstraps: probes delegation surfaces, names itself `<project emoji> GainsLog` and asks the user to pin it, writes docs/agents/hq.md with a header and one open workstream row for csv-export , and hands the user a titled paste-ready kickoff brief that primes the sub-thread with plugin skills (codex-first/autoreview/babysit/handoff) and a report shape. It does not implement the export itself.",
      "files": [
        "evals/fixtures/eval0"
      ],
      "assertions": [
        "Ledger created at docs/agents/hq.md with a header naming the HQ thread as `<project emoji> GainsLog` (proper casing, no 'HQ ·' prefix, no 📌) and the probed delegation level",
        "Ledger has exactly one open workstream row for the CSV export, titled `GainsLog · <workstream>` with no emoji",
        "Reply hands the user a copyable kickoff brief plus a thread title (level 2), rather than spawning a hidden subagent or doing the work itself",
        "Kickoff brief names plugin skills the sub-thread should invoke (at least autoreview or babysit, and handoff) and specifies the report shape back to HQ",
        "No implementation of the CSV export exists in the project diff (no new source files, no edits to src/)",
        "Reply tells the user to pin/rename the thread as HQ and states the operating contract briefly"
      ]
    },
    {
      "id": 1,
      "prompt": "/orchestrate-threads\n\nThe csv-export thread just said:\n\n- Workstream: `🔩 gainslog · csv-export`\n- State: needs decision\n- Artifact: PR #212\n- What changed: babysit reports merge-ready, both bugbot threads resolved. Export currently includes soft-deleted sets because listWorkouts() returns them; bugbot flagged it.\n- What HQ must decide: should soft-deleted sets be excluded from the export? If yes I need one more commit.",
      "expected_output": "HQ reads the existing ledger instead of relying on chat memory, updates the csv-export row (state merge-ready, last report today, waiting on: decision about soft-deleted sets), keeps the other two rows intact, lists the decision in Blocked on the human (or decides it if derivable and hands the user the answer to paste since Claude Code cannot message the thread), does not merge, and replies in the per-workstream shape with the single next thing.",
      "files": [
        "evals/fixtures/eval1"
      ],
      "assertions": [
        "Ledger csv-export row updated: state reflects merge-ready and last report dated today; other two workstream rows preserved unchanged",
        "The soft-deleted-sets question appears in the ledger's Blocked on the human list or is answered with a concrete decision handed to the user to paste into the thread",
        "Reply does not merge PR #212 or instruct the agent to merge; landing is left to the user",
        "Reply follows the per-workstream shape: what changed by workstream name, what is blocked on the human, and a single next step",
        "No attempt to rewrite the ledger from chat memory: hq.md still contains the sync-rewrite and duplicate-sets rows",
        "Re-orient wrote only docs/agents/hq.md: the README typo the ledger claims fixed is reported as an inconsistency, not fixed"
      ]
    },
    {
      "id": 2,
      "prompt": "/orchestrate-threads gainslog. Two things: the README says 'progresive', fix that. And src/sync/engine.ts needs a proper rewrite, it has three retry paths and shared cursor state — let's get going on that.",
      "expected_output": "HQ bootstraps, fixes the one-word README typo inline and logs it under Inline edits in the ledger, and does NOT rewrite the sync engine: it opens a plan workstream (kind plan) whose direct kickoff brief asks it to settle the target design and report findings, with build threads later once decisions are settled; tracker availability does not select a ticket workflow. Ledger has one open row, one inline edit, no engine.ts changes.",
      "files": [
        "evals/fixtures/eval2"
      ],
      "assertions": [
        "README typo 'progresive' fixed inline and the edit is logged under Inline edits in docs/agents/hq.md",
        "src/sync/engine.ts is unchanged; the sync rewrite is not implemented in this session",
        "Ledger has one open workstream row for the sync rewrite with kind plan and a thread title `GainsLog · <workstream>` with no emoji",
        "The kickoff brief asks the sync workstream to investigate and settle the design before implementation, without requiring wayfinder, to-spec, tickets, or tracker setup",
        "Reply hands the user a titled, copyable kickoff brief (level 2) rather than spawning a hidden subagent"
      ]
    },
    {
      "id": 3,
      "name": "bootstrap-no-tracker",
      "prompt": "/orchestrate-threads Personal research. Fresh project, no issue tracker configuration and no first work item. The runtime offers visible tasks. Describe the bootstrap actions without executing tools.",
      "expected_output": "Create/name HQ and its ledger, record tracker none, and finish bootstrap without setup or tickets.",
      "files": [],
      "assertions": [
        "Missing tracker does not block bootstrap or trigger setup.",
        "No issue creation is proposed."
      ]
    },
    {
      "id": 4,
      "name": "middagsro-small-feature",
      "prompt": "HQ for Middagsro: delegate this request: \"I handlingslista där man bockar av, ska man kunna lägga till egna varor\" (add custom items to the existing checkable shopping list). Keep changes local for review. No tickets and no tracker config. Describe the brief and ledger state without executing tools.",
      "expected_output": "Direct brief for adding custom shopping-list items, with verification and untracked reporting. Use direct build when scope is clear, or ordinary planning to settle material details; no tracker or formal-spec prerequisite. Completed local implementation stays awaiting delivery.",
      "files": [],
      "assertions": [
        "Addresses adding custom items to the existing checkable shopping list, not implementing checkboxes.",
        "Uses a direct build or planning brief without a tracker, formal-spec, or issue-creation gate.",
        "Respects no tickets and local-only delivery; completed implementation stays open awaiting delivery."
      ]
    },
    {
      "id": 5,
      "name": "general-planning",
      "prompt": "HQ: delegate research on ways to organize our family meal planning, compare tradeoffs, and return a recommendation in this conversation. No software repo or tracker. Describe the brief without executing tools.",
      "expected_output": "Direct research/plan brief defining the question, constraints, evidence and expected report; ledger tracks it without ticket skills.",
      "files": [],
      "assertions": [
        "Research and planning proceed without a tracker or setup.",
        "No mandatory wayfinder/to-spec/to-tickets chain."
      ]
    },
    {
      "id": 6,
      "name": "tracker-available-not-selected",
      "prompt": "HQ: our repo has a GitHub issue tracker configured. Delegate a small experiment comparing two onboarding layouts and report findings locally. No issue or tracked-delivery workflow was requested; active project instructions do not require one. Describe the brief without executing tools.",
      "expected_output": "Direct untracked planning/prototype brief; existing configuration does not authorize creating an issue.",
      "files": [],
      "assertions": [
        "Does not infer issue creation authority from delegation or tracker availability.",
        "Keeps the experiment and report untracked."
      ]
    },
    {
      "id": 7,
      "name": "explicit-tickets-missing-config",
      "prompt": "HQ: I want this migration managed as tickets and explicitly choose to-tickets after we settle the spec. There is no tracker configuration yet. What can proceed, and what is needed before publishing tickets? Do not execute tools.",
      "expected_output": "Continue independent planning; request only the chosen tracker setup and preserve to-tickets configuration/invocation requirements before ticket operations.",
      "files": [],
      "assertions": [
        "The ticket-dependent step waits for required configuration.",
        "Independent planning continues; no full machine/plugin/hook audit is required.",
        "Does not silently replace the explicitly chosen ticket workflow with untracked delivery."
      ]
    },
    {
      "id": 8,
      "name": "tracked-report-update",
      "prompt": "HQ assigned work on existing ticket TEAM-42 with status and progress-comment updates explicitly authorized. A build report now says implementation and tests are complete, changes remain local, but the ticket still says not started. Describe HQ reconciliation without executing tools.",
      "expected_output": "Inspect artifact evidence, have the owning task reconcile permitted ticket status/comment updates, retain the implementation in Open workstreams awaiting delivery.",
      "files": [],
      "assertions": [
        "Keeps the owned ticket current and reconciles report mismatch within granted permissions.",
        "Does not create replacement issues or archive before delivery."
      ]
    }
  ]
}
