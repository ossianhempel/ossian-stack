# GainsLog

Expo + Convex + WatermelonDB strength-training app with bidirectional sync.

- Package manager: pnpm. `pnpm test`, `pnpm typecheck`.
- Default development branch is `develop`; `main` is releases only. Never push to `main`.
- Use `trash`, never `rm`.
- Run `pnpm docs:list` before coding.
